import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#22499c",
      light: "skyblue",
    },
    secondary: {
      main: "#dfghrt",
      light: "lightgreen",
    },
  },
});

